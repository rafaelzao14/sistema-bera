import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
  },

  containerList: {
    alignItems: "center",
    justifyContent: "center",
    paddingTop: 32,
    paddingBottom: 40,
    marginBottom: 16,
  },
  endList: {
    margin: 8,
  },
});
