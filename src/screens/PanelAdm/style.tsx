import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "column",
  },
  endList: {
    padding: 24,
    alignSelf: "center",
    marginBottom: 32,
  },
});
