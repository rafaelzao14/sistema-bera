import { StyleSheet } from "react-native";

export const styleKeyboard = StyleSheet.create({
  keyboard: {
    flex: 1,
    flexDirection: "column",
    marginTop: 78,
    // maxWidth: 400,
    // maxHeight: 450,
  },
  safeView: {
    paddingTop: 40,
  },
});
