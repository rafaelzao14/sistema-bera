import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
  },
  containerList: {
    alignItems: "center",
    width: 414,
    paddingTop: 32,
    paddingBottom: 48,
  },
});
