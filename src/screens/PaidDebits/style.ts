import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    height: "100%",
    // backgroundColor: "red",
  },
  containerList: {
    alignItems: "center",
    backgroundColor: "#b2b2",
    maxHeight: "100%",
  },
});
