import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    height: "100%",
    // backgroundColor: "red",
  },
  containerList: {
    alignItems: "center",

    maxHeight: "100%",
  },
});
