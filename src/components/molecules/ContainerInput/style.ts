import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    borderBottomWidth: 1,
    borderColor: "#FFC225",
    maxHeight: 40,
    maxWidth: 266,
    marginTop: 16,
    alignItems: "center",
    justifyContent: "center",
  },
});
