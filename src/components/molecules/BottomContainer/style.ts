import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  containerBottom: {
    flexDirection: "column",
    alignItems: "center",
    marginTop: 48,
    minHeight: 100,
  },
});
