import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  input: {
    flex: 1,
    flexDirection: "row",
    backgroundColor: "#ffffff",
    alignItems: "center",
    maxHeight: 32,
    marginLeft: 8,
  },
});
