import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  input: {
    flex: 1,
    minHeight: 32,
    maxHeight: 40,
    maxWidth: 300,
    width: "100%",

    marginTop: 8,
    marginBottom: 16,

    paddingLeft: 8,

    borderWidth: 1,
    borderRadius: 8,
    borderColor: "#FFF",
    backgroundColor: "#FFF",
  },
});
