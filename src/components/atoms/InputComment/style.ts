import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  input: {
    height: 32,
    width: 320,

    marginTop: 32,
    marginBottom: 16,

    borderBottomWidth: 1,
    borderColor: "#FFF",
    backgroundColor: "none",
  },
});
