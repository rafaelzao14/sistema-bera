import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  button: {
    flex: 1,
    maxWidth: 230,
    maxHeight: 30,
  },
  text: {
    color: "#DD9C03",
    marginTop: 16,
  },
});
