import React from "react";

import { Image, Svg } from "react-native-svg";

const IconDebits = () => {
  return (
    <Svg>
      <Image href={require("../../../assets/beerPagas.png")} />
    </Svg>
  );
};
export default IconDebits;
