import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    maxWidth: 80,
    maxHeight: 80,
  },
  number: {
    fontSize: 60,
  },
  text: {
    fontSize: 24,
    bottom: 20,
    alignSelf: "center",
  },
});
