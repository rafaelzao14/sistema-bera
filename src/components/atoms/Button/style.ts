import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  button: {
    backgroundColor: "#DD9C03",
    height: 40,
    width: 270,
    borderRadius: 16,
    alignItems: "center",
    justifyContent: "center",
    marginTop: 17,
  },
  text: {
    color: "#FFF",
  },
});
