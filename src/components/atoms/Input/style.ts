import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  input: {
    flex: 1,
    flexDirection: "row",
    backgroundColor: "none",
    alignItems: "center",
    height: 40,
    marginLeft: 8,
    marginBottom: 4,
    maxWidth: 300,
  },
});
