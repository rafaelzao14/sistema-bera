import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  error: {
    flexDirection: "row",
    color: "red",
    fontSize: 11,
    // padding: 8,
    // marginLeft: 8,
  },
  container: {
    alignItems: "center",
    flex: 1,
    marginTop: 2,
  },
});
