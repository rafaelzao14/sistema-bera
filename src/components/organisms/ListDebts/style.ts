import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 80,
  },

  containerList: {
    alignItems: "center",
    justifyContent: "center",
    marginTop: 62,
  },
});
