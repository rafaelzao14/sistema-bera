import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  modal: {
    flex: 1,
    backgroundColor: "red",
    justifyContent: "center",
    alignItems: "center",
  },
});
