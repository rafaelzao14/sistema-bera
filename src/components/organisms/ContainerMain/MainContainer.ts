import { StyleSheet } from "react-native";
export const style = StyleSheet.create({
  containerMain: {
    flex: 1,
  },
  backgroundImg: {
    flex: 1,
    flexDirection: "column",
    alignItems: "center",
  },
});
